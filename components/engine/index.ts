// Engine components barrel — empty intentionally
// The engine lives in /lib, not /components
// This directory is reserved for engine-visualizing components
// (e.g. EquationDisplay, FormulaRenderer) added in Phase 4
export {};
